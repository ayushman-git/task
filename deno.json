{
    "tasks": {
      "create": "deno run --allow-read=. --allow-write=. src/index.ts create 'Priority check' -p=high -s=done",
      "ls": "deno run --allow-read=. --allow-write=. src/index.ts tasks",
      "rm": "deno run --allow-read=. --allow-write=. src/index.ts rm 3"
    }
  }