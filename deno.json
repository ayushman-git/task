{
    "tasks": {
      "create": "deno run --allow-read=. --allow-write=. src/index.ts create 'second message' -s=doing",
      "ls": "deno run --allow-read=. --allow-write=. src/index.ts tasks"
    }
  }